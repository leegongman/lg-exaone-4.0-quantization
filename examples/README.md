# Examples

This directory is reserved for minimal, public examples such as loading the public EXAONE reference model, applying a documented configuration pattern, or invoking a cleaned evaluation helper.

Examples must not require DACON access, private data, local absolute paths, private checkpoints, or custom wheel artifacts.
