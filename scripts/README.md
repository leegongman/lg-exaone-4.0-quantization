# Scripts

This directory is reserved for cleaned helper scripts that can run without private access. Candidate additions include local-evaluation wrappers, benchmark-summary generation, and configuration validation.

Before adding a script, remove credentials, DACON tokens, private URLs, local absolute paths, generated data paths, notebook output, and copied upstream source.
