# Experiment Manifests

This directory will hold sanitized experiment manifests and result summaries. It is not a location for raw logs, checkpoints, notebook outputs, or private leaderboard exports.

Every future result table must label values as `official`, `unverified-internal`, `local-benchmark`, `failed`, `partial`, or `exploratory`.
