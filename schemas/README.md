# Schemas

This directory will contain versioned public schemas for sanitized experiment manifests, benchmark summaries, and configuration examples.

Schemas should describe metadata, not embed private dataset fields, credentials, raw checkpoint paths, or competition submission payloads.
